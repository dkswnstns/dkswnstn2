

 1. print( ) 함수
    정의:()안에 내용을 콘솔창에 출력
    사용방법:print("출력할 값")
2. .int().str(),float()함수
    정의:
      int()= ()안에 수를 정수로 바꾼다
      str=()= ()안에 수를 문자로 바꾼다
      float()= ()안에 수를 실수로 바꾼다
    사용방법:()안에 수를 넣는다
 3.인덱싱이란
   정의: 원하는 부분의 문자를 가져오는 것이다
   사용방법: 문자변수이름[시작값+끝값+간격]
  4.슬라리싱
      정의:원하는 부분의 문자를 가져오는 것이다
      사용방법:문자변수이름[시작값+끝값+간격]
  5.replace()함수
      정의:변수 값을 바꾸는 것이다
      사용방법:변수이름.replace("바꿀값,"바꾼 값")